/* Add Two Values In C by Aadesh Lokhande */
#include<stdio.h>
void main()
{
    int a = 10, b = 20, c;          /* Declaring Variable */
    c = a + b;                      /* Adding 2 numbers */
    clrscr();                       /* Clear screen */
    printf("%d + %d = %d ",a,b,c);  /* Printing the answer */
    getch();                        /* Holding the output screen */
}