#include<stdio.h>
int main()
{
    // FILE *file;
    // file = fopen("AadeshCode.c","w");
    // fprintf(file,"#include<stdio.h>\n");
    // fclose(file);

    int num;
    printf("Enter a number = ");
    scanf("%d",&a);
    if(num==1)
    {
        printf("Even Number");
    }
    else if(num==2)
    {
        printf("Odd Number");
    }
}